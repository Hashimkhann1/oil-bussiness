import '../../res/paths/paths.dart';


class PurchasingView extends StatelessWidget {
  const PurchasingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
